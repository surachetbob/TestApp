import streamlit as st

# ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="Insurance Plan Selector", layout="centered")

st.title("📋 ระบบตรวจสอบแผนประกันภัย")
st.subheader("กรุณากรอกข้อมูลเพื่อดูรายละเอียดความคุ้มครอง")

# --- ส่วนที่ 1: รับข้อมูลผู้ใช้ ---
with st.container():
    col1, col2 = st.columns(2)
    with col1:
        name = st.text_input("ชื่อ-นามสกุล")
        age = st.number_input("อายุ", min_value=0, max_value=100, value=30)
    with col2:
        gender = st.selectbox("เพศ", ["ชาย", "หญิง"])
        plan_choice = st.selectbox("เลือกแผนประกัน", ["AIANPA2500", "AIANPA3000", "AIANPA3800"])

submit_button = st.button("Submit (ตรวจสอบข้อมูล)")

# --- ส่วนที่ 2: ตรรกะเงื่อนไขและข้อมูลแผนประกัน ---
# ข้อมูลจำลอง (คุณสามารถเปลี่ยนตัวเลขตรงนี้ได้ตามจริง)
plan_details = {
    "AIANPA2500": {"limit": 250000, "description": "เน้นความคุ้มครองพื้นฐาน ราคาย่อมเยา"},
    "AIANPA3000": {"limit": 500000, "description": "แผนระดับกลาง คุ้มครองครอบคลุมอุบัติเหตุทั่วไป"},
    "AIANPA3800": {"limit": 1000000, "description": "แผนสูงสุด คุ้มครองเต็มรูปแบบและค่ารักษาพยาบาลระดับสูง"}
}

# --- ส่วนที่ 3: การแสดงผลเมื่อกดปุ่ม ---
if submit_button:
    if name == "":
        st.warning("กรุณากรอกชื่อของคุณด้วยครับ")
    else:
        st.divider()
        st.success(f"สวัสดีคุณ {name}")
        
        # แสดงข้อมูลสรุปเบื้องต้น
        st.write(f
