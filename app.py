import streamlit as st

# 1. ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="PA Calculator", layout="centered")

# 2. ข้อมูลแผนประกัน
PLAN_BENEFITS = {
    "AIANPA2500": {
        "เสียชีวิต/ทุพพลภาพ": "500,000",
        "ค่ารักษาพยาบาล": "20,000",
        "อายุที่รับประกัน": "15 วัน - 70 ปี"
    },
    "AIANPA3000": {
        "เสียชีวิต/ทุพพลภาพ": "600,000",
        "ค่ารักษาพยาบาล": "35,000",
        "อายุที่รับประกัน": "15 วัน - 74 ปี"
    },
    "AIANPA3800": {
        "เสียชีวิต/ทุพพลภาพ": "500,000",
        "ค่ารักษาพยาบาล": "50,000",
        "อายุที่รับประกัน": "16 - 60 ปี"
    }
}

# 3. ฟังก์ชันคำนวณเบี้ย
def get_premium(plan, age):
    if plan == "AIANPA2500":
        if age <= 60: return 2500
        elif age <= 65: return 2700
        elif age <= 70: return 3150
        else: return 4000
    elif plan == "AIANPA3000":
        if age <= 60: return 3000
        elif age <= 65: return 3250
        elif age <= 70: return 3750
        else: return 4700
    elif plan == "AIANPA3800":
        if age <= 60: return 3800
        elif age <= 65: return 4600
        elif age <= 70: return 5300
        else: return 6770
    return 0

# 4. ส่วนการแสดงผล (UI)
st.title("🛡️ PA Insurance")
st.write("คำนวณเบี้ยประกันอุบัติเหตุ")

# ส่วนรับข้อมูล (เน้นแนวตั้งเพื่อมือถือ)
name = st.text_input("ชื่อ-นามสกุล", placeholder="กรุณาระบุชื่อ")
age = st.number_input("อายุ (ปี)", min_value=0, max_value=75, value=25)
plan_choice = st.selectbox("เลือกแผนประกันภัย", list(PLAN_BENEFITS.keys()))

# ปุ่มคำนวณ (ใช้ปุ่มเดียวชัดๆ)
if st.button("คำนวณเบี้ยและดูความคุ้มครอง"):
    premium = get_premium(plan_choice, age)
    
    st.divider()
    
    # แสดงเบี้ยประกัน
    st.subheader("💰 เบี้ยประกันของคุณ")
    st.success(f"รายปี: {premium:,} บาท")
    
    # ตรวจสอบเงื่อนไขอายุพิเศษ
    if plan_choice == "AIANPA3800" and age < 16:
        st.warning("⚠️ แผนนี้สำหรับอายุ 16 ปีขึ้นไปเท่านั้น")

    # แสดงตารางความคุ้มครอง
    st.write(f"### รายละเอียดแผน {plan_choice}")
    benefits = PLAN_BENEFITS[plan_choice]
    
    # แปลงข้อมูลเป็น List เพื่อแสดงผล
    display_data = []
    for k, v in benefits.items():
        display_data.append({"รายการ": k, "ความคุ้มครอง": v})
    
    st.table(display_data)

    # ปุ่มติดต่อ (Mobile Friendly)
    st.markdown("---")
    st.write("📞 **สนใจสอบถามเพิ่มเติม:**")
    st.markdown(f"**คุณมีน:** [086-5348448](tel:0865348448)")
